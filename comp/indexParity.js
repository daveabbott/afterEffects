// Reports index parity of a layer.
// Useful for adding limited variation to duplicate layers.
// Result is one of two values: Odd = 1. Even = 0.

index%2

// Example of use.

if (index%2 == 1) x = 10 else x = 20;